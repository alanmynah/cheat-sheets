# Taking docker to prod

## Examples and links

[link to multi-step docker builds]()

[`dev-prod-container-example`](./dev-prod-container-example) is an example of using nginx for prod with a container running the app
